#include "main.h"

/**
 * _abs - Calculates the absolute value of an integer
 * @n: - the integer being calculated
 * int - Represents an integer number
 * Return: Absolute value
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
