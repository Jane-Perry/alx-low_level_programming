#include "main.h"

/**
 * int _abs(int) - Calculates the absolute value of an integer
 * @int: - Represents an integer number
 * @ _abs: Denotes the total value of an integer
 * Return: Absolute value
 */

int _abs(int)
{
	int n;

	if (n >= 0)
		return (n);
	else
		return (-n);
}
